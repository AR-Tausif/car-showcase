import Image from "next/image";

const CustomButton = () => {
  return (
    <button className="custom-btn bg-purple-700 text-white rounded-lg">
      there ones
    </button>
  );
};

export default CustomButton;
